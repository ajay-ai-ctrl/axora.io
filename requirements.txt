# SmartKhet — Python Dependencies
# ==================================
# Install: pip install -r requirements.txt
# Python  : 3.11+

# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9           # File upload support

# ── Database ───────────────────────────────────────────────────────────────────
asyncpg==0.29.0                   # Async PostgreSQL
sqlalchemy[asyncio]==2.0.29       # ORM (for migrations)
alembic==1.13.1                   # DB migrations
motor==3.4.0                      # Async MongoDB
redis[hiredis]==5.0.4             # Redis client with C extension
timescaledb==0.0.1                # TimescaleDB helpers

# ── ML — Core ─────────────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
xgboost==2.0.3
imbalanced-learn==0.12.2          # SMOTE for class imbalance
joblib==1.4.2                     # Model serialisation

# ── ML — Deep Learning ────────────────────────────────────────────────────────
torch==2.3.0
torchvision==0.18.0
torchaudio==2.3.0

# ── ML — Computer Vision ──────────────────────────────────────────────────────
Pillow==10.3.0
opencv-python-headless==4.9.0.80  # No GUI deps — for server use

# ── ML — NLP / Speech ─────────────────────────────────────────────────────────
transformers==4.40.2
datasets==2.19.1
tokenizers==0.19.1
openai-whisper==20231117
soundfile==0.12.1
librosa==0.10.2
spacy==3.7.4

# ── ML — Forecasting ──────────────────────────────────────────────────────────
prophet==1.1.5
statsmodels==0.14.2

# ── ML — Model Export ─────────────────────────────────────────────────────────
onnx==1.16.0
onnxruntime==1.18.0
onnx-tf==1.10.0
skl2onnx==0.16.0                  # sklearn → ONNX
tensorflow==2.16.1                # For TFLite converter
tensorflow-hub==0.16.0

# ── MLOps ─────────────────────────────────────────────────────────────────────
mlflow==2.12.1
ray[serve,tune]==2.20.0           # Distributed ML + model serving

# ── Event Streaming ───────────────────────────────────────────────────────────
aiokafka==0.11.0                  # Async Kafka producer/consumer

# ── AWS ────────────────────────────────────────────────────────────────────────
boto3==1.34.91
botocore==1.34.91

# ── Auth ───────────────────────────────────────────────────────────────────────
PyJWT==2.8.0
cryptography==42.0.7
passlib[bcrypt]==1.7.4

# ── HTTP ───────────────────────────────────────────────────────────────────────
httpx==0.27.0
aiohttp==3.9.5

# ── Validation & Serialisation ────────────────────────────────────────────────
python-dateutil==2.9.0
pytz==2024.1

# ── Monitoring & Logging ──────────────────────────────────────────────────────
prometheus-fastapi-instrumentator==6.1.0
structlog==24.1.0
sentry-sdk[fastapi]==1.45.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.1.1
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0                     # Test client for FastAPI
factory-boy==3.3.0                # Test data factories
respx==0.21.1                     # Mock httpx calls

# ── Dev Tools ─────────────────────────────────────────────────────────────────
black==24.4.2                     # Code formatter
ruff==0.4.3                       # Fast linter
mypy==1.10.0                      # Type checking
pre-commit==3.7.0                 # Git hooks
